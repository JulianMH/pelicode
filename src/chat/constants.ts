/** Maximum amount of tool output included in the chat display. */
export const MAX_DISPLAY_BYTES = 100_000;

/** Maximum raw response body included in an OpenRouter error message. */
export const MAX_OPENROUTER_RESPONSE_BYTES = 8_000;
